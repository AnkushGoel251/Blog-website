<%- include('partials/header.ejs'); %>

    <h1 style="font-family: 'Merriweather', serif;">Home</h1>
    <p class="home"><%= home %></p>
    <% posts.forEach(function(post){ %>
       
    <h1><%= post.title %></h1>
    <p class="home"><%= post.content.substring(0,70) +"..." %><a href="http://localhost:3000/posts/<%= post.title%>">Read More</a></p>
    <% }) %>
</div>
